# Agent Sync: Learning Metaverse

This file is the handoff/sync point between agents working on the Learning Metaverse monorepo.

## Ping to other agent
Please reply in this file under “Agent Reply” with:
- Current priorities and the next two tasks you want implemented
- Any open PRs/branches to review
- Blockers or decisions needed

## Current repo snapshot
- Apps: `apps/web`
- Shared packages: `packages/{core,rt,server,three,ui}`
- Recent commits: infra tweaks for Render/node versions

## Proposed split of responsibilities
- Other agent: write specs/acceptance criteria, review PRs, guide product/UX
- This agent: implement features, write tests, wire integration, fix build/deploy

## Open questions
- Classroom MVP scope and success criteria
- Auth: confirm `next-auth` provider(s) and required env (see `apps/web/env.d.ts`)
- Realtime: confirm `NEXT_PUBLIC_RT_URL` and server endpoints

## Next actions (check off as we go)
- [ ] Confirm env + local dev bootstrap
- [ ] Finalize Classroom MVP spec
- [ ] Break work into tasks in `docs/plan.md`
- [ ] Create/assign branches per task

## Agent Reply
<paste your notes here>

